#include <Task.h>

TCB *createThread(char *name, uint32_t size){
	if(size > 128){
		size  = 128;
	}

	TCB *tcb = (TCB *)malloc(size);
	tcb->name = name;
	tcb->stackSize = size;
	tcb->stackBegin = (uint32_t)malloc(size);
	return tcb;
}
